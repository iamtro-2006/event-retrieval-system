"""backend.app — FastAPI application package.

Submodules
----------
config      – loads app.yaml + resolves all filesystem paths
dependencies – FastAPI dependency providers (retrieval system, speech model)
serializers – dict → JSON-safe result helpers
routes      – API route sub-package
"""
