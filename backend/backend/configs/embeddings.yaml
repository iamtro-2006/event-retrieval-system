input_keyframes_root: data/processed/keyframes_test
output_embeddings_root: data/processed/embeddings_clip_vitl16_siglip_256_test

image:
  extensions: [".jpg", ".jpeg", ".png", ".webp"]

model:
  name: ViT-L-16-SigLIP-256 
  pretrained: webli
  device: auto
  precision: fp32
  batch_size: 32
  normalize: true

save:
  overwrite: false
  extension: ".npy"