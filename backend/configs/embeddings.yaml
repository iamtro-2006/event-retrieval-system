input_keyframes_root: data/processed/keyframes
output_embeddings_root: data/processed/embeddings_clip_vitb16_quickgelu

image:
  extensions: [".jpg", ".jpeg", ".png", ".webp"]

model:
  name: ViT-B-16-quickgelu
  pretrained: metaclip_fullcc
  device: auto
  precision: fp32
  batch_size: 16
  normalize: true

save:
  overwrite: true
  extension: ".npy"